# WP13：Taro 前端低风险接入

> 执行口径：继续按 `docs/implementation/MVP_PLAN_WP06_PLUS.md` 的 MVP 范围执行。本包只接一个最小 V2 查询流：证书下拉 + 选中证书后考试场次/日期刷新；不重构全页面，不改完整生成主流程。

## 需求追踪矩阵

| ID | 原始要求 | 来源文件/行 | 实现位置 | 测试位置 | 验证证据 | 状态 |
|---|---|---|---|---|---|---|
| R01 | 创建统一 httpClient 和 ApiError，统一 Authorization/requestId/错误解析 | `05-API与前端接入.md` 当前 Taro 项目改造；`MVP_PLAN_WP06_PLUS.md` WP13 | `src/services/httpClient.ts`; `src/types/api.ts` | `npm run build:h5`; typecheck/lint scoped review | H5 build OK；typecheck 无本次 src 错误；lint 无新增 error/warning | done |
| R02 | 证书下拉切到 `/api/v2/certificates`，UI 使用 `code` 作为 value | `05-API与前端接入.md` 证书下拉；`NEXT_TASK.md` WP13 | `src/services/certificateApi.ts`; `src/hooks/useCertPresets.ts`; `src/pages/workbench/components/ExamArticle/index.tsx` | `npm run build:h5`; typecheck/lint scoped review | V2 默认开启，`ExamCategory.id = cert.code`；H5 build OK | done |
| R03 | 选择证书后请求 `/api/v2/certificates/{code}/exam-events`，考试日期来自后端，不再只靠前端常量 | `05-API与前端接入.md` 证书下拉；`07-按步骤实施计划.md` WP13 行动 | `src/services/certificateApi.ts`; `src/hooks/useCertPresets.ts`; `ExamArticle/index.tsx` | `npm run build:h5`; typecheck/lint scoped review | `resolveExamDate()` 调用 V2 exam-events，失败时回退列表 nextExam 日期 | done |
| R04 | 增加 feature flag，关闭时可回到旧 `policy-api /query/certs` | `07-按步骤实施计划.md` WP13 验证；`MVP_PLAN_WP06_PLUS.md` WP13 | `src/services/certificateApi.ts`; `src/hooks/useCertPresets.ts`; `.env.*` | `npm run build:h5`; static review | `TARO_APP_USE_V2_CERTIFICATE_API=false` 走旧 `fetchLegacyCerts()` | done |
| R05 | 删除前端硬编码 Dify API Key，改为环境变量读取；本包不改生成协议 | `05-API与前端接入.md` 配置；安全门禁 | `src/services/dify.ts`; `.env.development`; `.env.production`; `.env.test`; `.env.example` | secret scan; H5 build | no hard-coded `app-...`; Dify key 仅从 `TARO_APP_DIFY_API_KEY` 读取 | done |
| R06 | 不修改内容生成主流程，不做全页面重构 | `NEXT_TASK.md` WP13 摘要；`MVP_PLAN_WP06_PLUS.md` WP13 暂缓 | 本文件说明 | 本文件说明 | MVP 裁剪说明 | done |

## MVP 不做项

- 不接 `/api/v2/generations` 和 SSE；生成后端化留 WP14。
- 不改选题分析页的类目常量；本包只验证“图文生成”证书下拉最小流。
- 不接真实登录态；当前用 `TARO_APP_ORG_CODE` 注入 MVP 身份头，默认开发值为 `org_teaching_materials`。
