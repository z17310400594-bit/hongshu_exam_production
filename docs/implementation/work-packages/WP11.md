# WP11：最小 staging/校验框架 MVP

- MVP 停止点：`feat: add import validation MVP`
- 当前执行口径：继续按 `docs/implementation/MVP_PLAN_WP06_PLUS.md` 执行；只打通 import_batch/validation_error 和少量 valid/invalid fixtures。
- 本包范围：导入批次、校验错误、P0/P1 阻断、重复文件幂等、最小 API。
- 暂缓：全量桌面 CSV 迁移、各 CSV staging 表、ID 映射、数据对账、业务待确认报告。

## 需求追踪矩阵

| ID | 原始要求 | 来源文件/行 | 实现位置 | 测试位置 | 验证证据 | 状态 |
|---|---|---|---|---|---|---|
| R01 | 创建 import_batch、validation_error | `MVP_PLAN_WP06_PLUS.md` WP11；`02-schema-v2.sql` ingestion 表 | `api/models/ingestion.py`; `db/migrations/versions/c6f1a2b9d304_wp11_import_validation.py` | `db/tests/test_wp11_constraints.py`; `api/tests/test_model_registry.py` | 专项 9 passed；全量 125 passed | done |
| R02 | import_batch 记录文件名、hash、操作者、状态、行数 | 字段字典 `ingestion.import_batch` | `ingestion.import_batch` fields；`api/services/import_validation.py` | `api/tests/test_import_validation.py`; `db/tests/test_wp11_constraints.py` | API 返回 filename/contentSha256/importedBy/status/row counts | done |
| R03 | validation_error 记录行号、字段、错误码、错误信息 | 字段字典 `ingestion.validation_error` | `ingestion.validation_error` fields；`ValidationError` model | `api/tests/test_import_validation.py`; `db/tests/test_wp11_constraints.py` | invalid fixture 返回 expected error codes | done |
| R04 | 任一 P0 错误必须使批次整体回滚/失败 | 字段字典；`07-按步骤实施计划.md` WP11 验证 | `mark_batch_failed_for_blocking_error` trigger；service blocking severity | `db/tests/test_wp11_constraints.py`; `api/tests/test_import_validation.py` | P0/P1 validation error 使 batch failed | done |
| R05 | valid fixtures 全成功 | `07-按步骤实施计划.md` WP11 验证 | `db/tests/fixtures_wp11.py`; `record_import_validation` | `api/tests/test_import_validation.py` | valid fixture → committed, 2 success rows, 0 errors | done |
| R06 | invalid fixtures 全命中 expected_error_code | `03-测试数据模板.md` expected_errors；WP11 验证 | `db/tests/fixtures_wp11.py`; service/API | `api/tests/test_import_validation.py` | invalid fixture 命中 `UQ_CERTIFICATE_ALIAS` 和 `FK_RULE_EVIDENCE` | done |
| R07 | 同一文件重复导入幂等 | `07-按步骤实施计划.md` WP11 验证 | `content_sha256` UNIQUE；service idempotent return | `api/tests/test_import_validation.py`; `db/tests/test_wp11_constraints.py` | 重复 hash 返回同 batch，`idempotent=true`，不重复写错误 | done |
| R08 | CSV 不直接 COPY 正式表，先进入 staging/校验框架 | `HANDOFF.md`; `03-测试数据模板.md` 导入原则 | 本包只提供 ingestion gate；未新增直接正式表导入脚本 | `api/tests/test_import_validation.py` | valid/invalid 均只写 ingestion 表 | done |
| R09 | 最小 API smoke | MVP 硬底线 | `POST /import-batches/validate`; `GET /import-batches/{batch_id}` | `api/tests/test_import_validation.py`; real Uvicorn smoke | anonymous 401；create 200；duplicate idempotent；get 200；unknown 404 | done |
| R10 | ORM registry / Alembic metadata 不假绿 | `SELF_TEST_GATE.md` 数据库与 Alembic 硬门禁 | `api/models/__init__.py`; `db/migrations/env.py`; `api/tests/test_model_registry.py` | registry/import/metadata diff/Alembic check | registry 包含 ingestion 表；diff_count=0；alembic check clean | done |

## 未实现 / 暂缓

- 各 CSV staging 表：按 MVP 暂缓，后续可拆 WP11A/WP11B 等子包。
- 全量桌面 CSV 导入、ID 映射、数据对账：按 MVP 暂缓。
- “原始 = 迁移 + 拒绝 + 合并”的完整对账：需要真实迁移数据后执行，当前不做。
- 业务待确认报告：当前只记录 P2/P3 validation_error，不生成独立报告。
