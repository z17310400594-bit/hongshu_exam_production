# 第二层落地操作手册 — 前端全链路操作用例

> 基于前端页面实际代码 + Dify Workflow Python 节点，覆盖全部 14 项验收标准。
> 测试环境：H5 (http://localhost:10086)，浏览器打开即可操作。

---

## 一、页面结构速览

```
┌──────────────────────────────────────────────────────────┐
│  LeftNav               │  主内容区（ExamArticle）          │
│  ┌────────────┐        │  ┌───────────┬─────────────────┐│
│  │ 内容生产    │◀ active │  │ 左栏 380px │ 右栏预览区       ││
│  │ 选题发现    │        │  │ 配置面板   │ 卡片网格 210px   ││
│  │ 更多功能    │        │  │           │ 每卡             ││
│  │ 设置       │        │  │           │                 ││
│  └────────────┘        │  └───────────┴─────────────────┘│
└──────────────────────────────────────────────────────────┘
```

**左栏（配置面板）**：
1. 当前角色 — 下拉选择
2. 考试类型 — 预设下拉 / 自定义输入
3. 考试日期 — 日期选择器
4. 卡片编排 — 预设按钮 + 拖拽排序
5. 卡片风格 — 3 种主题切换
6. 高级选项 — 折叠面板
7. ✨ 生成内容 — 主操作按钮

**右栏（预览区）**：
- 工具栏：📥 导出全部 PNG / 📄 导出单张 / 📦 分层导出
- 卡片网格：210×280px 卡片预览

---

## 二、操作流程总览

```
选择角色 → 选择考试 → 确认日期 → 编排卡片 → 选择风格
    │
    ▼
点击「✨ 生成内容」
    │
    ▼
确认弹窗（核对参数）
    │
    ▼
进度遮罩（AI 生成中）
    │
    ▼
审核弹窗（文本审核/编辑/敏感词/查重）
    │
    ▼
确认无误 → 卡片预览区渲染
    │
    ▼
导出 PNG / 单卡重写 / 版本切换
```

---

## 三、详细测试用例

### TC-01：页面初始化

| 项目 | 预期值 |
|------|--------|
| 默认角色 | `小红书-医考号` |
| 默认考试 | `执业医师资格证` |
| 默认日期 | `2026-08-02` |
| 默认卡片序列 | 6 张标准预设：`cover → plan → plan → priority → resources → cta` |
| 默认风格 | `医考蓝` (medical-blue) |
| 右侧预览 | 6 个虚线骨架占位卡片，显示"占位" |

**操作**：刷新页面 → 验证初始状态

---

### TC-02：角色切换

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击"当前角色"下拉 | 显示 3 个选项 |
| 2 | 选择 `小红书-考研号` | 角色切换成功，下拉显示新值 |
| 3 | 选择 `公众号主编` | 角色切换成功 |
| 4 | 切回 `小红书-医考号` | 恢复默认 |

**Dify 联动**：角色参数通过 `role` 字段传入 Dify，CODE_A 中 `ROLE_PROFILES` 映射不同 persona/tone/phrases/banned 规则。

---

### TC-03：考试预设选择（含推荐卡组自动套用）

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击"考试类型"下拉 | 显示 5 个预设考试 + "-- 选择预设考试或手动输入 --" + "+ 自定义输入" |
| 2 | 选择 `执业医师资格证` | 考试名自动填充，日期自动填充 `2026-08-02`，卡组自动更新为推荐预设 `[cover, plan, plan, priority, mnemonics, cta]` |
| 3 | 选择 `护士资格证` | 日期 `2026-07-15`，卡组 `[cover, plan, subjects, resources, cta]` |
| 4 | 选择 `教师资格证` | 日期 `2026-10-25`，卡组 `[cover, plan, subjects, notice, cta]` |
| 5 | 选择 `注册安全工程师` | 日期 `2026-10-25`，卡组 `[cover, plan, subjects, mnemonics, resources, cta]` |
| 6 | 选择 `消防工程师` | 日期 `2026-11-07`，卡组 `[cover, plan, subjects, priority, resources, cta]` |

**关键验证**：每个考试的 `recommendedPreset` 字段已在 `exam-categories.ts` 中配置，选择后立即生效。

---

### TC-04：自定义考试输入

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 下拉选择 `+ 自定义输入` | 出现"考试名称（自定义）"文本框 |
| 2 | 输入 `执业中药师` | 文本框显示输入值 |
| 3 | 日期选择 `2026-10-18` | 日期更新 |
| 4 | 切回预设 `执业医师资格证` | 自定义文本框隐藏，值恢复为预设 |

**边界验证**：自定义模式下不留空考试名点生成 → 应弹出 alert "请选择考试类型或输入关键字"。

---

### TC-05：卡片编排 — 预设按钮

| 预设名 | 按钮文本 | 预期序列（6 种类型） |
|--------|----------|---------------------|
| 6 张标准 | `6 张标准` | cover → plan → plan → priority → resources → cta |
| 5 张精简 | `5 张精简` | cover → plan → subjects → notice → cta |
| 计划型 | `📅 计划型` | cover → plan → plan → priority → resources → cta |
| 知识型 | `📚 知识型` | cover → subjects → priority → mnemonics → resources → cta |

**操作**：依次点击 4 个预设按钮 → 验证卡片编排区 chip 列表更新。

---

### TC-06：卡片编排 — 手动添加/删除

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击 `+ 添加卡片` | 弹出 prompt 对话框 |
| 2 | 输入 `mnemonics` | 新增 🧠 记忆口诀 chip 到末尾 |
| 3 | 输入 `resources` | 新增 📦 备考资料 chip |
| 4 | 输入无效类型 `xxx` | 不添加（`ALL_CARD_TYPES.includes` 校验） |
| 5 | 点某 chip 的 `×` | 删除该卡片 |
| 6 | 尝试删到只剩 2 张 | `removeCard` 限制 `length <= 2` 时不动 |

---

### TC-07：卡片编排 — 拖拽排序

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 按住第 2 个 chip (`plan`) 拖到第 4 个位置 | chip 顺序更新 |
| 2 | 拖拽后检查右侧预览 | 骨架占位顺序同步更新 |

---

### TC-08：风格切换

| 风格 | 预期 CSS 变量效果 |
|------|------------------|
| 医考蓝 (medical-blue) | 标题色 `#1A6FB5`，强调色 `#E85D3A`，圆角 10px |
| 学霸粉 (study-pink) | 标题色 `#C82D6B`，强调色 `#FF6B9D`，圆角 14px |
| 极简灰 (minimal-gray) | 标题色 `#1A1A1A`，强调色 `#3B82F6`，圆角 4px |

**操作**：点击 3 种风格选项 → 观察卡片样式变化（生成前无可见效果，生成后有效）。

---

### TC-09：生成确认弹窗

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 填写完整参数后点击 `✨ 生成内容` | 弹出确认弹窗 |
| 2 | 检查弹窗内容 | 显示：考试名、考试日期、倒计时天数、卡片数量、卡片序列、风格 |
| 3 | 点击 `返回修改` | 弹窗关闭，回到配置面板 |
| 4 | 点击 `确认，开始生成` | 弹窗关闭，进度遮罩出现 |

---

### TC-10：AI 生成进度（需 Dify API 连通）

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 确认生成 | 出现白色半透明进度遮罩 |
| 2 | 观察进度步骤 | 依次显示：步骤 1 🎯 正在调用 AI / 步骤 3 ⏳ AI 正在逐张生成 / 步骤 5 ✅ 卡片生成完成 |
| 3 | 等待完成 | 遮罩消失，进入审核弹窗 |

**Dify 请求体验证**（抓包或日志）：

```json
{
  "inputs": {
    "exam_name": "执业医师资格证",
    "exam_date": "2026-08-02",
    "cards": "[{\"type\":\"cover\",...},...]",
    "role": "小红书-医考号",
    "countdown_days": <前端精确计算值>,
    "phase_1_end": "<YYYY-MM-DD>",
    "phase_2_end": "<YYYY-MM-DD>",
    "today_date": "<YYYY-MM-DD>"
  },
  "response_mode": "streaming",
  "user": "workbench-user"
}
```

**关键验证**：`countdown_days` / `phase_1_end` / `phase_2_end` / `today_date` 4 个字段由前端 `computeExamTimeVars()` 确定性计算，不受 Dify 服务器时区影响。

---

### TC-11：文本审核弹窗

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 生成完成后 | 自动弹出审核弹窗，标题「📋 内容审核」 |
| 2 | 检查卡片列表 | 每张卡片显示：类型 badge + 序号 + 纯文本内容 + 📋复制 + ✏️编辑 |
| 3 | 点击 `复制全文` | 全部卡片纯文本复制到剪贴板 |
| 4 | 点某卡的 `📋 复制` | 该卡片文本复制到剪贴板 |
| 5 | 若有校验错误 | 卡片块红框 + 🔴/🟡 标签标注问题 |

---

### TC-12：审核 — 敏感词修改

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 确认"敏感词"按钮选中（默认） | 按钮高亮紫色边框 |
| 2 | 在反馈框输入敏感词（如 `上岸`） | |
| 3 | 点击 `提交修改意见` | 对应卡片中该词替换为 `**` |
| 4 | 检查修改计数 | "已修改 N 次"数字递增 |
| 5 | 点击 `↩ 回退到初始版本` | 恢复 AI 原始内容，修改计数清零 |

---

### TC-13：审核 — 查重修改

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击"查重"按钮 | 按钮高亮 |
| 2 | 在反馈框输入重复关键词 | |
| 3 | 点击 `提交修改意见` | 匹配卡片的 subtitle 追加"（已重写避免重复）"，items content 追加"（内容已调整）" |

---

### TC-14：审核 — 手动编辑卡片

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击某卡的 `✏️ 编辑` | 卡片展开编辑表单 |
| 2 | 修改标题 / 副标题 | 对应 textarea/input 显示新值 |
| 3 | plan 类型：修改 days 文本 | textarea 显示"日期 周几 · 任务（时长）"格式 |
| 4 | items 类型（subjects/notice/resources/priority/mnemonics）：修改 items | textarea 显示"label：content"格式 |
| 5 | 点击 `✅ 确认修改` | 编辑内容合并到 pendingContent |
| 6 | 点击 `❌ 取消` | 确认后放弃编辑 |

**关键验证**：第 4 步 items 编辑涵盖全部 5 种 items 型卡片（subjects/notice/resources/priority/mnemonics），不是仅旧的 2 种。

---

### TC-15：审核通过 → 卡片预览

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击 `确认无误，下一步` | 审核弹窗关闭 |
| 2 | 检查右侧预览区 | 骨架占位消失，真实卡片内容渲染 |
| 3 | 检查卡片操作按钮 | hover 时右上角出现 🔄 重写按钮 |
| 4 | 卡片数量匹配 | 预览卡片数 = 配置的 cardSequence 长度 |

---

### TC-16：8 种卡片类型渲染验证

| 类型 | 场景 | 预期渲染元素 |
|------|------|-------------|
| **cover** 封面 | 生成后首张 | 标题（假设句式"假如你从 X.X 开始备考..."）、副标题（含倒计时天数）、居中大字倒计时 |
| **plan** 学习计划 | 含 days 数据 | 标题（"基础阶段 · 第 1-N 天"）、副标题、前 8 天日程表（日期/任务/时长）、"... 共 N 天" |
| **subjects** 考试科目 | 含 items 数据 | 标题、副标题、横向 tag 列表、详细条目（label + content） |
| **notice** 考前须知 | 含 items 数据 | 标题、副标题、编号列表（圆形序号 + label + content） |
| **cta** 行动引导 | 生成后末尾 | 大字 title、副标题（共情+提问+暗示）、二维码占位、"评论区互动留资" |
| **resources** 🆕 备考资料 | 含 items 数据 | 标题"备考资料清单"、副标题、左侧橙色边框的资料条目（📄 xxx.pdf + 用途说明）、底部"📥 以上资料都整理好了" |
| **priority** 🆕 分值分布 | 含 items 数据 | 标题"备考优先级指南"、副标题、排名列表（圆形序号+科目+分值+策略），按分值红/黄/灰三色 |
| **mnemonics** 🆕 记忆口诀 | 含 items 数据 | 标题"记忆口诀速记"、副标题、紫色虚线边框口诀卡片（引号包裹口诀 + 拆解映射 chip） |

---

### TC-17：封面倒计时实时精准验证（验收项 #1）

| 操作 | 预期 |
|------|------|
| 选择今天之后 30 天的考试日期 → 生成 | 封面 subtitle 显示"距离考试仅剩 30 天" |
| 改成明天 → 生成 | 显示"距离考试仅剩 1 天" |
| 改成昨天（已过期）→ 生成 | `computeExamTimeVars` 中 `countdownDays` 为 0，CODE_1 兜底为 0 |

**验证点**：`getCountdown()` 和 `computeExamTimeVars()` 都在前端计算，不受 Dify 服务器时间影响。

---

### TC-18：Plan 三阶段窗口精确验证（验收项 #2）

| 操作 | 预期 |
|------|------|
| 选 `2026-08-02`（距今天约 52 天），生成含 2 张 plan 卡 | 第 1 张 plan：基础阶段 today→phase1End（考前 55 天，但已不足则 today+3 天后）；第 2 张 plan：强化冲刺 phase1End+1→考试日 |
| 选 `2027-06-01`（距今天 > 300 天），生成含 2 张 plan 卡 | 第 1 张：基础阶段 today→考前55天；第 2 张：强化阶段 考前55天→考前20天（因 plan_count=2 已用了 2 张，冲刺阶段由最后一张承载） |

**验证点**：`computeExamTimeVars()` 返回的 `phase1End`/`phase2End` 由前端计算，传入 Dify CODE_1 后精确覆盖均分逻辑。

---

### TC-19：Plan 任务具体到章节验证（验收项 #3）

Dify CODE_A plan Prompt 中明确要求：
- ✅ `「生理学·血液循环」「病理学·细胞适应与损伤」`
- ❌ `「复习生理」「做题」「学习」`

**验证**：审核弹窗中 plan 型卡片的 days 文本 → task 字段应为"学科·章节"格式。

---

### TC-20：Plan 含复盘日验证（验收项 #4）

CODE_A plan Prompt 要求每 5-6 天插入：
- `☑ 复盘日 · 整理框架 · 整理错题`（每 5-6 天）
- `休息 ♨️`（每 12-14 天）

**验证**：审核弹窗中检查 days 列表 → 应出现"☑ 复盘日"或"休息 ♨️"。

---

### TC-21：Plan 时长递增验证（验收项 #5）

CODE_A plan Prompt 规则：
- 基础阶段：前 1/3 天 50min → 中间 1/3 天 70min → 后 1/3 天 90min
- 强化阶段：90min → 105min → 120min
- 冲刺阶段：全程 120min

**验证**：同一 plan 卡片的 days 前中后三段 duration 应递增。

---

### TC-22：封面句式验证（验收项 #6）

CODE_A cover Prompt：
- ✅ 标题以 `假如你从 X.X 开始备考XXX！` 开头
- ❌ 禁止 `XX 备考指南`、`通关秘籍`、`必过攻略`、`上岸计划`

**验证**：所有 cover 卡片的 title 必须以"假如你从"开头。

---

### TC-23：resources 卡片验证（验收项 #7）

| 验证项 | 预期 |
|--------|------|
| items 数量 | 6-8 个 |
| label 格式 | `YYYY 考试简称资料名.pdf` |
| content | ≤15 字用途说明 |
| 覆盖需求 | 三色笔记、记忆口诀、母题/真题、打卡表、模拟卷、思维导图 |
| title | 固定"备考资料清单" |
| subtitle | 考试名称 |

---

### TC-24：priority 卡片验证（验收项 #8）

| 验证项 | 预期 |
|--------|------|
| items 数量 | 4-6 个 |
| 排序方式 | 按性价比从高到低 |
| label 格式 | `① 单元名（约 XX 分）`，含分值信号 |
| content | 差异化备考策略 |
| 最后一条 | 战略性放弃建议 |
| 颜色分级 | ≥25 分 🔴tier-high / 10-24 🟡tier-mid / <10 ⚪tier-low |

---

### TC-25：mnemonics 卡片验证（验收项 #9）

| 验证项 | 预期 |
|--------|------|
| items 数量 | 3-5 条 |
| label | 6-15 字口诀（谐音、画面感） |
| content | 逐字拆解映射，用 `→` 连接 |
| 样式 | 紫色虚线边框 + 引号包裹 + chip 标签拆解 |
| 引入语 | 可选一句人味引入语 |

---

### TC-26：导出功能

| 操作 | 预期 |
|------|------|
| 点击 `📥 导出全部 PNG` | Toast 显示"📥 正在导出全部卡片..."→"✅ 导出完成"，触发 html2canvas 批量截图 |
| 点击 `📄 导出单张` | 弹出 prompt 输入序号 → Toast 显示单张导出进度 |
| 点击 `📦 分层导出` | Toast 显示分层导出进度（每卡 3 层透明 PNG） |
| 未生成时点导出 | 按钮 disabled 状态 |

---

### TC-27：单卡重写

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | hover 某卡片 → 点击右上角 🔄 | 弹出重写弹窗 |
| 2 | 输入反馈（如"加周末复习建议"） | |
| 3 | 点击 `重写此卡` | Mock 模式：subtitle 追加"（含周末复习建议）"；真实 API 模式：调用 Dify 单卡重写 Workflow |
| 4 | 检查版本切换 | 卡片底部出现 V1 / V2 切换按钮 |
| 5 | 点击 V1 | 恢复原始版本 |
| 6 | 点击 V2 | 切到重写版本 |

---

### TC-28：向后兼容验证（验收项 #13）

| 操作 | 预期 |
|------|------|
| 点击"5 张精简"预设（仅旧 5 种类型）→ 生成 | 正常生成，无报错 |
| CODE_2 校验 | `cover/plan/subjects/notice/cta` 仍在白名单中 |
| fallback_titles | 旧 5 种兜底标题不变 |

---

### TC-29：CODE_1 节点验证（Dify 侧）

| 场景 | 预期 |
|------|------|
| 前端传入 countdown_days=52 | CODE_1 使用 52，不重新计算 |
| 前端未传 countdown_days（或=0） | CODE_1 fallback 到 `datetime` 计算 |
| 前端传入 today_date="2026-06-11" | CODE_1 使用该日期而非 `datetime.now()` |
| 前端传入 phase_1_end/phase_2_end + plan_count≥2 | 使用精确三阶段窗口，不用均分逻辑 |
| 前端未传 phase_1_end | fallback 到均分逻辑 |

---

### TC-30：CODE_2 节点验证（Dify 侧）

| 场景 | 预期 |
|------|------|
| LLM 返回 resources 类型卡片 | type 在白名单中，校验通过 |
| LLM 返回 priority 类型卡片 | type 在白名单中，校验通过 |
| LLM 返回 mnemonics 类型卡片 | type 在白名单中，校验通过 |
| title 为空 | 使用 fallback_titles 兜底：resources→"备考资料清单"、priority→"备考优先级指南"、mnemonics→"记忆口诀速记" |
| LLM 返回未知类型 | assert 抛出 `Invalid type` 异常 |

---

### TC-31：CODE_A 节点验证（Dify 侧）

| 场景 | 预期 |
|------|------|
| current_card.type = "resources" | 走 resources 分支，system_prompt 含 6 种核心需求 + 6-8 个 pdf 清单格式 |
| current_card.type = "priority" | 走 priority 分支，system_prompt 含分值排序 + 差异化策略 + 战略性放弃 |
| current_card.type = "mnemonics" | 走 mnemonics 分支，system_prompt 含口诀风格 + 逐字拆解映射 |
| current_card.type = "cover" | 走 cover 分支（公式版），system_prompt 含"假如你从"句式 + 三阶段定位 |
| current_card.type = "plan" | 走 plan 分支（已优化），system_prompt 含三阶段拆分 + 时长递增 + 复盘日 |
| current_card.type = "subjects"/"notice"/"cta" | 走对应分支，保持不变 |
| role = "小红书-考研号" | ROLE_PROFILES 映射考研号 persona，务实风格、少 emoji |
| role = "公众号主编" | ROLE_PROFILES 映射主编 persona，专业风格、几乎无 emoji |

---

## 四、验收标准对照表（来自手册第十节）

| # | 验收项 | 对应用例 | 通过条件 |
|---|--------|----------|----------|
| 1 | 封面倒计时实时精准 | TC-17 | 选今天+30天→显示"仅剩30天"；明天→"仅剩1天" |
| 2 | Plan 三阶段窗口精确 | TC-18 | 2 张 plan 卡分别显示基础阶段和强化冲刺的精确窗口 |
| 3 | Plan 任务具体到章节 | TC-19 | task 为"生理学·血液循环"级别 |
| 4 | Plan 含复盘日 | TC-20 | 每 5-6 天出现"☑ 复盘日"或"休息 ♨️" |
| 5 | Plan 时长递增 | TC-21 | 同阶段内 50min→90min/120min |
| 6 | 封面句式为"假如你从" | TC-22 | title 以"假如你从 X.X 开始备考..."开头 |
| 7 | resources 卡片可生成 | TC-23 | 6-8 个带 .pdf 的资料清单 |
| 8 | priority 卡片含分值+策略 | TC-24 | label 含分值，content 含策略，最后一条为战略性放弃 |
| 9 | mnemonics 卡片含映射 | TC-25 | label 为口诀，content 含逐字拆解映射 |
| 10 | 新卡片类型预览渲染正常 | TC-16 | resources/priority/mnemonics 样式正常无错乱 |
| 11 | 审核编辑支持新类型 | TC-14 | 审核弹窗中 resources/priority/mnemonics 可编辑 items |
| 12 | TypeCheck 零错误 | TC-32 | `npm run typecheck` 无 src/ 下的 TS 报错 |
| 13 | 向后兼容 | TC-28 | 仅用旧 5 种类型仍可正常生成 |

---

## 五、边界与异常场景

### TC-33：空考试名生成
- **操作**：清空考试名 + 日期 → 点生成
- **预期**：alert "请选择考试类型或输入关键字"

### TC-34：考试已过期
- **操作**：日期选昨天 → 生成
- **预期**：倒计时显示"已过期"，countdown_days=0

### TC-35：卡片数上限
- **操作**：连续添加卡片至 9 张以上
- **预期**：MAX_CARDS=9（代码中定义但无硬阻断），ALL_CARD_TYPES 共 8 种

### TC-36：Dify API 失败
- **操作**：断网或 API Key 无效时生成
- **预期**：`generateCards` 抛出异常，`fetch` 非 200 时 throw Error

### TC-37：LLM 返回格式错误
- **场景**：CODE_A LLM 输出非 JSON 或有 Markdown 包裹
- **预期**：CODE_2 解析时自动清洗 Markdown 代码块，parse 失败则生成 `_error` 标记的兜底卡片

### TC-38：LLM 返回卡片数量不匹配
- **场景**：LLM 返回 5 张但预期 6 张
- **预期**：CODE_2 按 expected_count 补空卡片，不阻断流程

---

## 六、执行顺序建议

```
Phase 1 — 前端无 Dify 验证（5 分钟）
  TC-01 → TC-02 → TC-03 → TC-04 → TC-05 → TC-06 → TC-07 → TC-08

Phase 2 — Dify 联调（30 分钟，需 API 连通）
  TC-09 → TC-10 → TC-11 → TC-12 → TC-13 → TC-14 → TC-15

Phase 3 — 卡片类型验证（20 分钟）
  TC-16 → TC-17 → TC-18 → TC-19 → TC-20 → TC-21 → TC-22
  → TC-23 → TC-24 → TC-25

Phase 4 — 导出与重写（10 分钟）
  TC-26 → TC-27

Phase 5 — 兼容与边界（10 分钟）
  TC-28 → TC-33 → TC-34 → TC-35

Phase 6 — 校验（5 分钟）
  TC-32: npm run typecheck && npm run lint
```

**总耗时**：约 1.5 小时（不含 Dify API 调用等待时间）。

---

## 七、当前状态说明

| 模块 | 状态 | 备注 |
|------|------|------|
| 前端类型 (`exam-article.ts`) | ✅ 已更新 | 8 种 CardType + DifyGenerateParams 含 3 个新计算字段 |
| 前端服务 (`dify.ts`) | ✅ 已更新 | `computeExamTimeVars()` + 4 个新 inputs 字段传入 Dify |
| 前端常量 (`card-templates.ts`) | ✅ 已更新 | 5 种预设（含 PRESET_PLAN_TYPE / PRESET_KNOWLEDGE_TYPE） |
| 前端常量 (`exam-categories.ts`) | ✅ 已更新 | 5 个考试均含 `recommendedPreset` |
| 前端组件 (`ExamArticle/index.tsx`) | ✅ 已更新 | 3 种新卡片渲染 + 类型标签 + 预设按钮 + items 编辑 |
| 前端样式 (`ExamArticle/index.scss`) | ✅ 已更新 | resources/priority/mnemonics 样式 + badge 颜色 |
| 前端校验 (`validation.ts`) | ✅ 已更新 | `getCardPlainText` 扩展至 5 种 items 型 |
| Dify CODE_1 | ✅ 已更新 | 接收 4 个新变量 + 阶段窗口精确覆盖逻辑 |
| Dify CODE_2 | ✅ 已更新 | 白名单扩展至 8 种 + fallback_titles 补充 |
| Dify CODE_A | ✅ 已更新 | 3 个新类型分支 + cover/plan Prompt 公式版 |
| Dify START 节点 | ⚠️ 需手动 | 在 Dify 后台添加 4 个输入变量 |
| Dify 变量传递 | ⚠️ 需手动 | 确认 CODE_1 → Iterator → CODE_A 变量透传 |
| TypeCheck | ✅ 通过 | src/ 零 TS 错误 |
| H5 Build | ✅ 通过 | webpack 编译成功 |
